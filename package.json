{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "mypublish": "lerna publish --contents ./packages/next --yes --skip-npm",
    "postmypublish": "git push && git push --tags"
  }
}
